info:
  name: Reception views live queue
  type: http
  seq: 9

http:
  method: GET
  url: http://localhost:8000/api/v1/visits/live-queue
  headers:
    - name: Authorization
      value: Bearer RECEPTION_TOKEN
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
