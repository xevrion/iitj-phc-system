info:
  name: Get attendance summary report
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8000/api/v1/admin/reports/attendance
  headers:
    - name: Authorization
      value: Bearer ADMIN_TOKEN
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
