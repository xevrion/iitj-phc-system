info:
  name: Patient views own appointments
  type: http
  seq: 3

http:
  method: GET
  url: http://localhost:8000/api/v1/appointments/my
  headers:
    - name: Authorization
      value: Bearer PATIENT_TOKEN
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
