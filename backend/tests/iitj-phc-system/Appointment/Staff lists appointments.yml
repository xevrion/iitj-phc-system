info:
  name: Staff lists appointments
  type: http
  seq: 6

http:
  method: GET
  url: http://localhost:8000/api/v1/appointments
  headers:
    - name: Authorization
      value: Bearer RECEPTION_TOKEN
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
