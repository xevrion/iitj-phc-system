info:
  name: Patient views own bills
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:8000/api/v1/bills/mine
  headers:
    - name: Authorization
      value: Bearer PATIENT_TOKEN
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
