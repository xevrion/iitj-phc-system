info:
  name: List upcoming PHC events
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8000/api/v1/events
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
