info:
  name: Patient views current visit
  type: http
  seq: 10

http:
  method: GET
  url: http://localhost:8000/api/v1/visits/my-current
  headers:
    - name: Authorization
      value: Bearer PATIENT_TOKEN
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
