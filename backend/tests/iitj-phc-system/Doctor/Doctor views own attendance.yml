info:
  name: Doctor views own attendance
  type: http
  seq: 7

http:
  method: GET
  url: http://localhost:8000/api/v1/doctors/me/attendance
  headers:
    - name: Authorization
      value: Bearer DOCTOR_TOKEN
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
