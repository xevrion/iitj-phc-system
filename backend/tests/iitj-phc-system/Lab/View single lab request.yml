info:
  name: View single lab request
  type: http
  seq: 5

http:
  method: GET
  url: http://localhost:8000/api/v1/lab-requests/LAB_REQUEST_ID
  headers:
    - name: Authorization
      value: Bearer TOKEN
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
