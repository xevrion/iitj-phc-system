# ─── Database ────────────────────────────────────────────────────────────────
# Prisma Postgres (cloud) — sign up at prisma.io/postgres, copy connection string
# Local PostgreSQL alternative documented in TESTING.md appendix
DATABASE_URL="prisma+postgres://accelerate.prisma-data.net/?api_key=YOUR_API_KEY"

# ─── Server ───────────────────────────────────────────────────────────────────
PORT=8000
NODE_ENV=development          # "development" | "production"

# ─── Auth ─────────────────────────────────────────────────────────────────────
# Must be a long, random string in production. Any string works in dev.
JWT_SECRET=change-this-to-a-long-random-secret
JWT_EXPIRY=7d                 # e.g. 7d, 24h, 60m

# ─── LDAP ─────────────────────────────────────────────────────────────────────
# Local development LDAP server:
#   cd ..
#   docker compose up -d ldap
LDAP_URL=ldap://127.0.0.1:1389
LDAP_BASE_DN=dc=iitj,dc=ac,dc=in
LDAP_USERS_OU=ou=users

# ─── CORS ─────────────────────────────────────────────────────────────────────
CORS_ORIGIN=http://localhost:5173

# ─── Security Hardening (Sprint 7) ───────────────────────────────────────────
# Generic API rate limit
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=300

# Stricter limit for auth endpoints such as login
AUTH_RATE_LIMIT_WINDOW_MS=600000
AUTH_RATE_LIMIT_MAX_REQUESTS=20

# In production behind a reverse proxy, set this to true to reject plain HTTP.
ENFORCE_HTTPS=false

# ─── Cloudinary File Uploads ─────────────────────────────────────────────────
CLOUDINARY_CLOUD_NAME=your-cloud-name
CLOUDINARY_API_KEY=your-api-key
CLOUDINARY_API_SECRET=your-api-secret
CLOUDINARY_FOLDER=iitj-phc-system/medical-documents
