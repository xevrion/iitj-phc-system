info:
  name: Delete document
  type: http
  seq: 3

http:
  method: DELETE
  url: http://localhost:8000/api/v1/patients/PATIENT_ID/documents/DOC_ID
  headers:
    - name: Authorization
      value: Bearer DOCTOR_TOKEN
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
