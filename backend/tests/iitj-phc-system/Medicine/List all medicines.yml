info:
  name: List all medicines
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8000/api/v1/medicines
  headers:
    - name: Authorization
      value: Bearer TOKEN
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
