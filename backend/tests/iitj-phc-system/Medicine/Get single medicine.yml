info:
  name: Get single medicine
  type: http
  seq: 2

http:
  method: GET
  url: http://localhost:8000/api/v1/medicines/MEDICINE_ID
  headers:
    - name: Authorization
      value: Bearer TOKEN
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
