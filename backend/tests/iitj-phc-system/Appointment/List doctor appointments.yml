info:
  name: List doctor appointments
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8000/api/v1/doctors/DOCTOR_ID/appointments
  headers:
    - name: Authorization
      value: Bearer TOKEN
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
