info:
  name: Doctor views own appointments
  type: http
  seq: 4

http:
  method: GET
  url: http://localhost:8000/api/v1/doctors/me/appointments
  headers:
    - name: Authorization
      value: Bearer DOCTOR_TOKEN
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
